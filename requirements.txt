vncdotool==1.2.0
paho-mqtt==2.1.0
Pillow==12.3.0
# pytesseract is no longer used at runtime (main.py pipes straight to the
# tesseract binary — see _tesseract()). Kept as the reference oracle that
# tests/test_ocr_no_tempfiles.py diffs the stdin/stdout path against.
pytesseract==0.3.13
prometheus_client==0.21.1
python-json-logger==3.2.1
python-dotenv==1.0.1
